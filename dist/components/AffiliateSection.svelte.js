/* src/components/AffiliateSection.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	space
} from "../../snowpack/pkg/svelte/internal.js";

import { showFireWorks } from "../common/index.js";

function create_fragment(ctx) {
	let section;
	let div0;
	let a0;
	let t2;
	let div3;
	let mounted;
	let dispose;

	return {
		c() {
			section = element("section");
			div0 = element("div");
			a0 = element("a");

			a0.innerHTML = `<figure><img src="https://d2gdx5nv84sdx2.cloudfront.net/uploads/pq6gukie/marketing_asset/banner/4835/ghost-squircle.png" alt="Ghost Pro" border="0" class="mb-2"/> 
                <figcaption class="font-medium hover:underline">Ghost (Pro)</figcaption></figure>`;

			t2 = space();
			div3 = element("div");

			div3.innerHTML = `<div>👈 This is an affiliate link for the best blog tool in the world.
            <br/>
            (Even I use it for my Blog!)</div> 
        <div>See
            <a class="text-white hover:underline" href="https://www.derpycoder.com/disclosure/" target="_blank" rel="noopener">Disclosure Policy</a>
            for more.</div>`;

			attr(a0, "id", "affiliate-link");
			attr(a0, "href", "https://ghost.org?via=derpycoder");
			attr(a0, "target", "_blank");
			attr(a0, "rel", "noopener");
			attr(div0, "class", "w-2/5 mr-2");
			attr(div3, "class", "flex flex-col justify-around w-4/5");
			attr(section, "class", "flex justify-center p-5 mb-2 mb-6 text-xs text-gray-900 bg-white\n    shadow-2xl bg-opacity-30 w-referral rounded-xl");
		},
		m(target, anchor) {
			insert(target, section, anchor);
			append(section, div0);
			append(div0, a0);
			append(section, t2);
			append(section, div3);

			if (!mounted) {
				dispose = listen(a0, "click", showFireWorks);
				mounted = true;
			}
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(section);
			mounted = false;
			dispose();
		}
	};
}

class AffiliateSection extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default AffiliateSection;