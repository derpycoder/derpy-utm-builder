module.exports = {
    purge: [],
    darkMode: 'class', // or 'media' or 'class' or false
    theme: {
        extend: {},
    },
    variants: {
        extend: {
            backgroundColor: ['even'],
        },
    },
    plugins: [],
}
