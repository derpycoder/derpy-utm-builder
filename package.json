{
  "scripts": {
    "init": "npx tailwindcss-cli@latest build -o ./public/tailwind.css",
    "start": "snowpack dev",
    "build": "snowpack build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.5.2",
    "snowpack": "^3.0.1",
    "gh-pages": "^3.1.0"
  },
  "dependencies": {
    "copy-text-to-clipboard": "^3.0.0",
    "svelte": "^3.32.3"
  }
}